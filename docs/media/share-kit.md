# Apple Notes Snapshot Share Kit

Use this page when you need the shortest reusable set of assets for explaining
the project in public.

## Punchline

Use upstream for the engine. Use this repo for the local control room.

Optional truthful hooks you can add when the audience cares:

- AI Diagnose = operator next-step explanation layer
- Local Web API = token-gated host-local browser/API surface
- MCP Provider = read-only agent-facing substrate

## 10-second demo

Show the three-step run flow:

- review the path
- trigger the first export
- install the scheduler and verify health

## Compare card

Reuse the upstream-vs-snapshot comparison when you need to explain where the
wrapper adds value.

If the audience is builder-heavy, also point them to:

- `docs/local-api/`
- `docs/for-agents/`
- `docs/mcp/`

## Release snippet template

- Why this matters:
- What changed for first-time visitors:
- AI Diagnose / Local Web API / MCP / builder entry:
- Upgrade / migration:
- Known limitations:
- Links:
